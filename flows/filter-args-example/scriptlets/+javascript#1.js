//#region generated meta
/**
 * @import { Context } from "@oomol/types/oocana";
 * @typedef {{
 *   input: string;
 * }} Inputs;
 * @typedef {{
 *   output: any[];
 * }} Outputs;
 */
//#endregion

/**
 * @param {Inputs} params
 * @param {Context<Inputs, Outputs>} context
 * @returns {Promise<Outputs>}
 */
export default async function (params, context) {

    // your code

    return { output: [1,2,3,4,5,6,7,8,9] };
}
